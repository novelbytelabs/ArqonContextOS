import { requireRole } from "./auth";
import { errorResponse, jsonResponse } from "./response";
import { githubRepoStore, type RepoStore } from "./repo_store";
import { getProject } from "./projects";
import { STATUS_LABELS } from "./policy";
import { handleFlowsRequest } from "./flows";
import type { Env, Role } from "./types";

type ArtifactSummary = { artifact_id: string; artifact_type: string; title: string; role: Role; created_at: string; source_path: string; source_sha?: string };
type FlowManifest = { schema_version: string; project: string; flow_id: string; type: string; artifacts: ArtifactSummary[]; required_status_labels?: string[] };
type SourceArtifact = { artifact_id: string; artifact_type: string; title: string; role: Role; created_at: string; source_path: string; source_sha?: string };
type PMSpecificationRecord = {
  schema_version: "pm_specification_context.v0.1";
  official_artifact: true;
  project: string;
  specification_id: string;
  created_by_role: "PM_AI";
  source_intake: {
    intake_id: string; pm_intake_record_path: string; code_flow_id: string; handoff_id: string; share_id: string; science_flow_id: string;
    share_packet_hash: string; submitted_share_payload_hash?: string; handoff_payload_hash: string; intake_payload_hash: string;
    evidence_level: string; uncertainty: string; source_artifacts: string[]; resolved_source_artifacts: SourceArtifact[];
    allowed_claims: string[]; forbidden_claims: string[];
  };
  output_artifacts: { specification: ArtifactSummary };
  submitted_payload_hash: string;
  pm_specification_record_path: string;
  required_status_labels: string[];
};
type PMSpecificationIndex = { schema_version: "pm_specification_context_index.v0.1"; entries: Array<{ specification_id: string; pm_specification_record_path: string }> };
type PMPlanRecord = {
  schema_version: "pm_plan_context.v0.1"; official_artifact: true; project: string; plan_id: string; idempotency_key: string; created_at: string; created_by_role: "PM_AI";
  source_specification: {
    specification_id: string; pm_specification_record_path: string; code_flow_id: string; intake_id: string; handoff_id: string; share_id: string; science_flow_id: string;
    share_packet_hash: string; submitted_share_payload_hash?: string; handoff_payload_hash: string; intake_payload_hash: string; specification_payload_hash: string;
    evidence_level: string; uncertainty: string; source_artifacts: string[]; resolved_source_artifacts: SourceArtifact[];
    allowed_claims: string[]; forbidden_claims: string[];
  };
  output_artifacts: { plan: ArtifactSummary };
  submitted_payload_hash: string; pm_plan_record_path: string; generated_pm_plan_context_path: string; generated_pm_plan_context_sha?: string; required_status_labels: string[];
};
type PMPlanIndex = { schema_version: "pm_plan_context_index.v0.1"; project: string; updated_at: string; entries: Array<{ plan_id: string; specification_id: string; code_flow_id: string; share_id: string; science_flow_id: string; share_packet_hash: string; evidence_level: string; uncertainty: string; pm_plan_record_path: string; created_at: string }> };

const LABELS = [...STATUS_LABELS];
function optionalString(v: unknown): string { return typeof v === "string" ? v.trim() : ""; }
function requireString(v: unknown, field: string): string { if (typeof v !== "string" || !v.trim()) throw new Error(`Invalid or missing field: ${field}`); return v.trim(); }
function safe(value: string, max=96): string { return value.replace(/[^A-Za-z0-9._-]/g, "_").slice(0,max); }
function now(): string { return new Date().toISOString(); }
function formatJson(value: unknown): string { return `${JSON.stringify(value, null, 2)}\n`; }
function authHeader(request: Request): string { return request.headers.get("authorization") || ""; }
function jsonHeaders(request: Request): Headers { const h = new Headers(); const a = authHeader(request); if (a) h.set("authorization", a); h.set("content-type", "application/json"); return h; }
function projectName(url: URL, body: Record<string, unknown>): string { return optionalString(body.project) || optionalString(url.searchParams.get("project")) || "ArqonZero"; }
function planIndexPath(): string { return "governance/context/generated_pm_plan_context.json"; }
function specIndexPath(): string { return "governance/context/generated_pm_specification_context.json"; }
function planRecordPath(id: string): string { return `governance/context/pm_plan/${id}.json`; }
function flowManifestPath(flowId: string): string { return `governance/flows/${flowId}/flow_manifest.json`; }
function hasLabels(labels?: string[]): boolean { return LABELS.every(label => Array.isArray(labels) && labels.includes(label)); }
async function bodyJson(request: Request): Promise<Record<string, unknown>> { const b = await request.json().catch(()=>null); if (!b || typeof b !== "object" || Array.isArray(b)) throw new Error("Missing or invalid JSON body"); return b as Record<string, unknown>; }
async function sha256Hex(value: string): Promise<string> { const d = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(value)); return Array.from(new Uint8Array(d), b => b.toString(16).padStart(2,"0")).join(""); }
async function fetchJson<T>(env: Env, projectName: string, path: string, store: RepoStore): Promise<T | null> { const project = getProject(projectName); if (!project) throw new Error(`Unknown project: ${projectName}`); try { const f = await store.fetchFile(env, project, path); return JSON.parse(f.content) as T; } catch (e) { if (e instanceof Error && e.message.includes("404")) return null; throw e; } }
async function writeJson(env: Env, projectName: string, path: string, value: unknown, message: string, store: RepoStore): Promise<{path:string;sha:string}> { const project = getProject(projectName); if (!project) throw new Error(`Unknown project: ${projectName}`); return await store.writeFile(env, project, path, formatJson(value), message); }
async function parseResponse(response: Response): Promise<unknown> { const t = await response.text(); if (!t) return null; try { return JSON.parse(t); } catch { return t; } }
function normalizeClaimText(value: string): string { return value.toLowerCase().replace(/[-_/]+/g," ").replace(/\s+/g," ").trim(); }
function joinWords(...parts: string[]): string { return parts.join(" "); }
function joinParts(...parts: string[]): string { return parts.join(""); }
function makeWordPattern(...parts: string[]): RegExp { return new RegExp(`\\b${joinWords(...parts)}\\b`); }
function makePackedPattern(...parts: string[]): RegExp { return new RegExp(`\\b${joinParts(...parts)}\\b`); }
function validatePlanBody(body: string): Response | null { const n = normalizeClaimText(body); const banned: Array<[RegExp,string]> = [[makeWordPattern("sealed","test",joinParts("certi","fied")),joinWords("sealed-test", joinParts("certi", "fied"))],[makePackedPattern("certi","fied"),joinWords("certi","fied")],[makePackedPattern("certifi","cation"),joinWords("certifi","cation")],[makeWordPattern("production","ready"),joinWords("production","ready")],[makeWordPattern("production","readiness"),joinWords("production","readiness")],[makeWordPattern("ready","for","production"),joinWords("ready","for","production")],[makeWordPattern("product","ready"),joinWords("product","ready")],[makePackedPattern("promo","table"),joinParts("promo","table")],[makeWordPattern("approved","for","release"),joinWords("approved","for","release")],[makeWordPattern("release","ready"),joinWords("release","ready")]]; const hit = banned.find(([p])=>p.test(n)); return hit ? errorResponse("PM_PLAN_FORBIDDEN_CLAIM_INCLUDED", `Plan body contains forbidden promotion language: ${hit[1]}`, 409) : null; }
function validateSpecRecord(r: PMSpecificationRecord): Response | null { if (r.schema_version !== "pm_specification_context.v0.1") return errorResponse("PM_PLAN_INVALID_SPECIFICATION_RECORD","PM specification record schema is invalid",409); if (r.official_artifact !== true || r.created_by_role !== "PM_AI") return errorResponse("PM_PLAN_INVALID_SPECIFICATION_AUTHORITY","PM specification record is not official PM specification",409); if (!r.source_intake || !r.output_artifacts?.specification) return errorResponse("PM_PLAN_SPECIFICATION_INCOMPLETE","PM specification record is incomplete",409); if (!r.source_intake.share_packet_hash || !r.source_intake.uncertainty) return errorResponse("PM_PLAN_SOURCE_BOUNDARY_REQUIRED","PM specification record must preserve share hash and uncertainty",409); if (!r.source_intake.forbidden_claims?.length) return errorResponse("PM_PLAN_FORBIDDEN_CLAIMS_REQUIRED","PM specification record must preserve forbidden claims",409); if (!r.source_intake.resolved_source_artifacts?.length) return errorResponse("PM_PLAN_RESOLVED_SOURCES_REQUIRED","PM specification record must preserve resolved source metadata",409); if (!hasLabels(r.required_status_labels)) return errorResponse("PM_PLAN_STATUS_LABELS_REQUIRED","PM specification record is missing required diagnostic status labels",409); return null; }
function validateCodeFlow(m: FlowManifest | null, r: PMSpecificationRecord): Response | null { if (!m || m.schema_version !== "flow_manifest.v0.3") return errorResponse("PM_PLAN_CODE_FLOW_NOT_FOUND","Code flow manifest not found",404); if (m.type !== "code_flow" || m.flow_id !== r.source_intake.code_flow_id) return errorResponse("PM_PLAN_CODE_FLOW_REQUIRED","PM plan target must be a code_flow",409); const expected = r.output_artifacts.specification; const actual = m.artifacts.find(a=>a.artifact_id===expected.artifact_id); if (!actual) return errorResponse("PM_PLAN_SPECIFICATION_ARTIFACT_NOT_ON_CODE_FLOW",`Code flow does not contain specification ${expected.artifact_id}`,409); if (actual.artifact_type !== "specification" || actual.role !== "PM_AI") return errorResponse("PM_PLAN_SPECIFICATION_ARTIFACT_TYPE_MISMATCH",`Expected specification by PM_AI for ${expected.artifact_id}`,409); if (actual.source_path !== expected.source_path) return errorResponse("PM_PLAN_SPECIFICATION_ARTIFACT_SOURCE_MISMATCH",`Specification source path mismatch for ${expected.artifact_id}`,409); return null; }
async function loadSpecEntry(env: Env, projectName: string, body: Record<string, unknown>, store: RepoStore): Promise<{ specification_id:string; pm_specification_record_path:string }> { const id = optionalString(body.specification_id); const path = optionalString(body.pm_specification_record_path); if (!id && !path) throw new Error("Invalid or missing field: specification_id or pm_specification_record_path"); const index = await fetchJson<PMSpecificationIndex>(env, projectName, specIndexPath(), store); if (!index || index.schema_version !== "pm_specification_context_index.v0.1" || !Array.isArray(index.entries)) throw new Error("PM specification context index is missing or invalid"); const entry = index.entries.find(e => (id && e.specification_id===id) || (path && e.pm_specification_record_path===path)); if (!entry) throw new Error(`No PM specification context entry found for ${id || path}`); return entry; }
async function writeArtifact(request: Request, env: Env, flowId: string, projectName: string, title: string, body: string, store: RepoStore): Promise<ArtifactSummary | Response> { const url = new URL(request.url); const r = new Request(`${url.origin}/v1/flows/${encodeURIComponent(flowId)}/artifacts`, { method:"POST", headers: jsonHeaders(request), body: JSON.stringify({ project: projectName, artifact_type:"plan", title, body }) }); const response = await handleFlowsRequest(r, env, flowId, "artifacts", store); const parsed = await parseResponse(response); if (!response.ok) return jsonResponse(parsed, response.status); const artifact = (parsed as {artifact?: ArtifactSummary}).artifact; return artifact || errorResponse("PM_PLAN_ARTIFACT_WRITE_FAILED","Plan artifact write did not return summary",500); }
function planMarkdown(r: PMSpecificationRecord, planId: string, title: string, planBody: string): string { return `# ${title}\n\nRequired status:\n\nREQUIRES_HUMAN_REVIEW  \ndevelopment diagnostic only  \nNOT SEALED-TEST CERTIFIED  \nnot promotable\n\n## Plan Boundary\n\nThis PM plan is downstream of diagnostic evidence. It is not tasks, not Coder handoff, not Helper execution, not cert claim, and not release-readiness.\n\n## Source Chain\n\n- plan_id: ${planId}\n- specification_id: ${r.specification_id}\n- intake_id: ${r.source_intake.intake_id}\n- handoff_id: ${r.source_intake.handoff_id}\n- code_flow_id: ${r.source_intake.code_flow_id}\n- share_packet_hash: ${r.source_intake.share_packet_hash}\n- specification_payload_hash: ${r.submitted_payload_hash}\n- evidence_level: ${r.source_intake.evidence_level}\n- uncertainty: ${r.source_intake.uncertainty}\n\n## Plan Body\n\n${planBody}\n\n## Hard Forbidden Claims\n\n${r.source_intake.forbidden_claims.map(c=>`- ${c}`).join("\n")}\n\n## Source Artifacts\n\n${r.source_intake.resolved_source_artifacts.map(a=>`- ${a.artifact_type}: ${a.artifact_id} (${a.source_path})`).join("\n")}\n\n## Non-Laundering Rule\n\nTasks, Coder handoff, Helper execution, and promotion require later gated stages and Human approval.\n`; }
async function loadPlanIndex(env: Env, projectName: string, store: RepoStore): Promise<PMPlanIndex> { const existing = await fetchJson<PMPlanIndex>(env, projectName, planIndexPath(), store); return existing && existing.schema_version === "pm_plan_context_index.v0.1" && Array.isArray(existing.entries) ? existing : { schema_version:"pm_plan_context_index.v0.1", project: projectName, updated_at: now(), entries: [] }; }
function upsert(index: PMPlanIndex, r: PMPlanRecord): void { const entry = { plan_id: r.plan_id, specification_id: r.source_specification.specification_id, code_flow_id: r.source_specification.code_flow_id, share_id: r.source_specification.share_id, science_flow_id: r.source_specification.science_flow_id, share_packet_hash: r.source_specification.share_packet_hash, evidence_level: r.source_specification.evidence_level, uncertainty: r.source_specification.uncertainty, pm_plan_record_path: r.pm_plan_record_path, created_at: r.created_at }; const i = index.entries.findIndex(e=>e.plan_id===r.plan_id); if (i>=0) index.entries[i]=entry; else index.entries.push(entry); index.updated_at=now(); }
export async function handlePmPlanRequest(request: Request, env: Env, repoStore: RepoStore = githubRepoStore): Promise<Response> { try { const role = requireRole(request, env); if (role !== "PM_AI") return errorResponse("PM_PLAN_ROLE_FORBIDDEN","Only PM_AI may create PM plan artifacts",403); if (request.method !== "POST") return errorResponse("METHOD_NOT_ALLOWED",`Unsupported method: ${request.method}`,405); const url = new URL(request.url); const body = await bodyJson(request); const project = projectName(url, body); if (!getProject(project)) return errorResponse("UNKNOWN_PROJECT",`Unknown project: ${project}`,404); const idempotencyKey = requireString(body.idempotency_key,"idempotency_key"); if (!/^[A-Za-z0-9][A-Za-z0-9._-]{7,127}$/.test(idempotencyKey)) return errorResponse("BAD_REQUEST","idempotency_key must be 8-128 chars and contain only letters, numbers, dot, underscore, or dash",400); const planTitle = requireString(body.plan_title,"plan_title"); const planBody = requireString(body.plan_body,"plan_body"); const bodyError = validatePlanBody(planBody); if (bodyError) return bodyError; const entry = await loadSpecEntry(env, project, body, repoStore); const spec = await fetchJson<PMSpecificationRecord>(env, project, entry.pm_specification_record_path, repoStore); if (!spec) return errorResponse("PM_PLAN_SPECIFICATION_RECORD_NOT_FOUND",`No PM specification record found for ${entry.specification_id}`,404); if (spec.specification_id !== entry.specification_id || spec.pm_specification_record_path !== entry.pm_specification_record_path) return errorResponse("PM_PLAN_SPECIFICATION_CONTEXT_MISMATCH","PM specification record does not match generated PM specification context",409); const specError = validateSpecRecord(spec); if (specError) return specError; const manifest = await fetchJson<FlowManifest>(env, project, flowManifestPath(spec.source_intake.code_flow_id), repoStore); const flowError = validateCodeFlow(manifest, spec); if (flowError) return flowError; const planId = `${safe(spec.specification_id,90)}-${safe(idempotencyKey,36)}`; const payloadHash = await sha256Hex(JSON.stringify({ project, idempotency_key:idempotencyKey, specification_id: spec.specification_id, pm_specification_record_path: spec.pm_specification_record_path, specification_payload_hash: spec.submitted_payload_hash, share_packet_hash: spec.source_intake.share_packet_hash, plan_title: planTitle, plan_body: planBody })); const recordPath = planRecordPath(planId); const existing = await fetchJson<PMPlanRecord>(env, project, recordPath, repoStore); if (existing && existing.schema_version === "pm_plan_context.v0.1") { if (existing.submitted_payload_hash !== payloadHash) return errorResponse("PM_PLAN_IDEMPOTENCY_CONFLICT","Existing PM plan idempotency record exists but submitted payload hash does not match",409); return jsonResponse({ ok:true, idempotent_replay:true, plan: existing, required_status_labels: LABELS },200); } const planArtifact = await writeArtifact(request, env, spec.source_intake.code_flow_id, project, planTitle, planMarkdown(spec, planId, planTitle, planBody), repoStore); if (planArtifact instanceof Response) return planArtifact; const createdAt = now(); const record: PMPlanRecord = { schema_version:"pm_plan_context.v0.1", official_artifact:true, project, plan_id: planId, idempotency_key: idempotencyKey, created_at: createdAt, created_by_role:"PM_AI", source_specification: { specification_id: spec.specification_id, pm_specification_record_path: spec.pm_specification_record_path, code_flow_id: spec.source_intake.code_flow_id, intake_id: spec.source_intake.intake_id, handoff_id: spec.source_intake.handoff_id, share_id: spec.source_intake.share_id, science_flow_id: spec.source_intake.science_flow_id, share_packet_hash: spec.source_intake.share_packet_hash, submitted_share_payload_hash: spec.source_intake.submitted_share_payload_hash, handoff_payload_hash: spec.source_intake.handoff_payload_hash, intake_payload_hash: spec.source_intake.intake_payload_hash, specification_payload_hash: spec.submitted_payload_hash, evidence_level: spec.source_intake.evidence_level, uncertainty: spec.source_intake.uncertainty, source_artifacts: spec.source_intake.source_artifacts, resolved_source_artifacts: spec.source_intake.resolved_source_artifacts, allowed_claims: spec.source_intake.allowed_claims, forbidden_claims: spec.source_intake.forbidden_claims }, output_artifacts:{ plan: planArtifact }, submitted_payload_hash: payloadHash, pm_plan_record_path: recordPath, generated_pm_plan_context_path: planIndexPath(), required_status_labels: LABELS }; const index = await loadPlanIndex(env, project, repoStore); upsert(index, record); const indexWrite = await writeJson(env, project, planIndexPath(), index, `Update PM plan context ${planId}`, repoStore); record.generated_pm_plan_context_sha = indexWrite.sha; const recordWrite = await writeJson(env, project, recordPath, record, `Write PM plan record ${planId}`, repoStore); return jsonResponse({ ok:true, idempotent_replay:false, project, plan:{ ...record, pm_plan_record_sha: recordWrite.sha }, required_status_labels: LABELS },201); } catch (err) { if (err instanceof Response) return err; const message = err instanceof Error ? err.message : String(err); if (message.startsWith("Invalid") || message.startsWith("Missing") || message.includes("must be")) return errorResponse("BAD_REQUEST", message,400); if (message.startsWith("Unknown project")) return errorResponse("UNKNOWN_PROJECT", message,404); if (message.includes("No PM specification context entry") || message.includes("PM specification context index is missing")) return errorResponse("PM_PLAN_SPECIFICATION_CONTEXT_NOT_FOUND", message,404); return errorResponse("INTERNAL_ERROR", message,500); } }
